angular
	.module('ylrent.rpt.services')
	.factory('DailyService', DailyService);

DailyService.$inject = ['$log'];

function DailyService($log) {

	var service = {};

	angular.extend(service, {

	});

	return service;
}